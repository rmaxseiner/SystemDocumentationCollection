# Infrastructure Documentation Collection - Environment Configuration
# Copy this file to .env and customize for your environment

# ============================================================================
# Docker Registry Configuration
# ============================================================================
REGISTRY=registry.maxseiner.casa

# ============================================================================
# Logging Configuration
# ============================================================================
# Valid levels: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================================================
# Collection Toggles
# ============================================================================
# Enable/disable specific collectors
COLLECT_DOCKER=true
COLLECT_PROXMOX=true
COLLECT_SYSTEM_DOCS=true
COLLECT_GRAFANA=false

# ============================================================================
# Volume Mount Paths (Host Side)
# ============================================================================

# SSH Keys - Path to directory containing SSH private keys for remote access
# Must contain keys for accessing Docker hosts, Proxmox servers, etc.
SSH_KEY_PATH=~/.ssh

# Configuration Files - Path to infrastructure-docs directory
# Should contain YAML config files defining systems to collect from
CONFIG_PATH=./infrastructure-docs

# RAG Output - Where processed RAG data will be written
# This is consumed by the MCP server (InfrastructureDocumentationMCP project)
RAG_OUTPUT_PATH=./rag_output

# Collected Data - Where raw collected data is stored
COLLECTED_DATA_PATH=./work/collected

# Logs - Where application logs are written
LOGS_PATH=./logs

# ============================================================================
# Optional: Graylog Integration
# ============================================================================
# Leave empty to disable Graylog logging
GRAYLOG_HOST=
GRAYLOG_PORT=12201

# ============================================================================
# Deployment Configuration
# ============================================================================
# Path where docker-compose.yml is deployed (used by Jenkins)
DEPLOY_PATH=/opt/infrastructure-docs-collector
